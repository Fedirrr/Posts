import React, { FC } from 'react';
import './header.css';

const Header: FC = () => {
    return (
        <div className="header">
            <h5 className="headerTitle">Posts</h5>
        </div>
    );
};

export default Header;
